<core:FragmentDefinition xmlns:form="sap.ui.layout.form"
    xmlns:core="sap.ui.core"
    xmlns="sap.m"
    xmlns:i="sap.ui.core"	 xmlns:f = "sap.ui.layout.form"
    xmlns:html="http://www.w3.org/1999/xhtml">
   
    <Label text="Correction 1"/>
    <Input value = "{appView>corrections1}" editable="{appView>/editableFields}"/>
    <Label text="Correction 2"/>
    <Input value = "{appView>corrections2}" editable="{appView>/editableFields}"/>
    <Label text="Correction 3"/>
    <Input value = "{appView>corrections3}" editable="{appView>/editableFields}"/>
                                
</core:FragmentDefinition>
<mvc:View controllerName="ent.ui.ecommerce.controller.Carborator"
    xmlns:mvc="sap.ui.core.mvc" xmlns:u="sap.ui.unified" displayBlock="true"
    xmlns:f="sap.ui.layout.form"
    xmlns:core="sap.ui.core"  
    xmlns:core.h="sap.ui.core.HTML"  
    xmlns="sap.m"
    xmlns:ux="sap.uxap">
    <Page enableScrolling="false"  showHeader="{appView>/visibleHeader}" id="SimpleForm-1">
        <content>

            <TextArea visible="false" id="_IDGenTextArea1" rows="30" cols="60" value="{appView>/jsonValue}"></TextArea>
            <HBox direction="{=${appView>/desktop} ? 'Row' : 'Column'}" alignItems="Center" alignContent="Center" justifyContent="Center" class="sapUiSmallMarginTop">
                <ComboBox   items="{appView>/userDetails}" id="idPopinLayout"
                    placeholder="Select User"
                    change="onPopinLayoutChanged"
                    class="sapUiTinyMarginEnd">
                    <items>
                        <core:Item enabled="true"  text="{appView>UserName}"
                            key="{appView>id}"/>
                    </items>
                </ComboBox>
                    <u:FileUploader
                    id="fileUploader"
                    sameFilenameAllowed="true"
                    class="sapUiTinyMarginBegin"
                    change="onFileUploaddChange"
                    icon="sap-icon://browse-folder"
                    buttonText="Browse"
                    fileType="xml,xlsx"
                    multiple="false" />  
                </HBox>
                <MessageStrip
                visible="{appView>/messageStripVis}"
				text="Same Job is already available"
				type="Warning"
				showIcon="true">
			</MessageStrip>
            <ux:ObjectPageLayout id="ObjectPageLayout" showAnchorBarPopover="false" showTitleInHeaderContent="true" preserveHeaderStateOnScroll="true"  isChildPage="true" upperCaseAnchorBar="false">
                
        <ux:headerTitle>
            
            <ux:ObjectPageDynamicHeaderTitle expandedContent="false">
                
            
            </ux:ObjectPageDynamicHeaderTitle>
        </ux:headerTitle>
        <ux:ObjectPageSection title="Product Details">
            <ux:subSections  >
                <ux:ObjectPageSubSection>
                    <ux:blocks>
                        <f:SimpleForm  layout="ColumnLayout"  columnsL="3" singleContainerFullSize="false">
                            <f:content >
                                <core:Fragment fragmentName="ent.ui.ecommerce.fragments.ProductDetails" type="XML"></core:Fragment>
                            </f:content>
                        </f:SimpleForm>
                    </ux:blocks>
                </ux:ObjectPageSubSection>
            </ux:subSections>
        </ux:ObjectPageSection>

        <ux:ObjectPageSection title="Paper and Layouts">
            <ux:subSections>
                <ux:ObjectPageSubSection>
                    <ux:blocks>
                        <f:SimpleForm editable="false" layout="ColumnLayout"  columnsL="3" singleContainerFullSize="false" >
                            <f:content>
                                <core:Fragment fragmentName="ent.ui.ecommerce.fragments.PaperAndLayout" type="XML"></core:Fragment>
                    
                            </f:content>
                        </f:SimpleForm>
                    </ux:blocks>
                </ux:ObjectPageSubSection>
            </ux:subSections>
        </ux:ObjectPageSection>

        <ux:ObjectPageSection title="Machine Details">
            <ux:subSections>
                <ux:ObjectPageSubSection>
                    <ux:blocks>
                        <f:SimpleForm editable="false" layout="ColumnLayout"  columnsL="3" singleContainerFullSize="false" >
                            <f:content>
                                <core:Fragment fragmentName="ent.ui.ecommerce.fragments.MachineDetails" type="XML"></core:Fragment>
                            </f:content>
                        </f:SimpleForm>
                    </ux:blocks>
                </ux:ObjectPageSubSection>
            </ux:subSections>
        </ux:ObjectPageSection>


        <ux:ObjectPageSection title="Cost">
            <ux:subSections>
                <ux:ObjectPageSubSection>
                    <ux:blocks>
                        <f:SimpleForm class="sapUiMediumMarginBottom" editable="false"    columnsXL="2" singleContainerFullSize="false" >
                            <f:content>
                                <core:Title text="Cost Structure" />
                                    
                                <core:Fragment fragmentName="ent.ui.ecommerce.fragments.CostStructure" type="XML"></core:Fragment>

                                    <core:Title text="Development Cost" />

                                    <core:Fragment fragmentName="ent.ui.ecommerce.fragments.DevelopmentCost" type="XML"></core:Fragment>
                    
                            </f:content>
                        </f:SimpleForm>
                    </ux:blocks>
                </ux:ObjectPageSubSection>
            </ux:subSections>
        </ux:ObjectPageSection>
    </ux:ObjectPageLayout>
            <!-- <f:SimpleForm visible="{appView>/simpleFormVisibility}" width="100%" editable="true" id="SimpleForm-1" columnsM="2"
			columnsL="3"
			columnsXL="4"  layout="ColumnLayout">
				<f:content>
						<core:Title></core:Title>
						<Label design="Bold" text="Job CardNo."></Label>
						<Text  text="{appView>jobCardNo}"/>
						<Label design="Bold" text="Po No."></Label>
						<Text text="{appView>poNo}"/>
						<Label design="Bold" text="Job Code"></Label>
						<Text text="{appView>jobCode}"/>
                        <Label design="Bold" text="Job Code" />
                        <Text  text="{appView>jobCode}" />
                        <Label design="Bold" text="Date" />
                        <Text text="{path:'appView>date', formatter: '.dateFormatter'}" />
                        <Label design="Bold" text="Artwork Code" />
                        <Text text="{appView>artworkCode}" />
                        <Label  design="Bold" text="Client PO No." />
                        <Text text="{appView>clientPONo}" />
                        <Label  design="Bold" text="Name of the Product" />
                        <Text text="{appView>nameOFTheProduct}" />
                        <Label  design="Bold" text="Industry" />
                        <Text text="{appView>industry}" />
                        <Label  design="Bold" text="Carton Type" />
                        <Text text="{appView>cartonType}" />
                        <Label  design="Bold" text="Quantity (pcs)" />
                        <Text text="{appView>qtyPcs}" />
                        <Label  design="Bold" text="Paper GSM" />
                        <Text text="{appView>PaperGSM}" />
                        <Label  design="Bold" text="Paper Quality" />
                        <Text text="{appView>paperQuality}" />
                        <Label  design="Bold" text="Printing" />
                        <Text text="{appView>printing}" />
                        <Label  design="Bold" text="Color" />
                        <Text text="{appView>color}" />
                        <Label  design="Bold" text="Size (L x W x H)" />
                        <Text text="{path: 'appView>sizeL', formatter: '.sizeFormatter'} x {path: 'appView>sizeW', formatter: '.sizeFormatter'} x {path: 'appView>sizeH', formatter: '.sizeFormatter'}" />
                        <Label  design="Bold" text="Varnish/Lamination" />
                        <Text text="{appView>varLmt}" />
                        <Label  design="Bold" text="Effects" />
                        <Text text="{appView>effects}" />
                        <Label  design="Bold" text="Lock" />
                        <Text text="{appView>lock}" />
                        <Label  design="Bold" text="Top Flap (TF)" />
                        <Text text="{appView>tF}" />
                        <Label  design="Bold" text="Panel Flap (PF)" />
                        <Text text="{appView>pF}" />
                        <Label  design="Bold" text="Double Cut" />
                        <Text text="{appView>doubleCut}" />
                        <Label  design="Bold" text="Trim TF" />
                        <Text text="{appView>trimTF}" />
                        <Label  design="Bold" text="Trim PF" />
                        <Text text="{appView>trimPF}" />
                        <Label  design="Bold" text="No. of Ups-1" />
                        <Text text="{appView>noOfUps-1}" />
                        <Label  design="Bold" text="No. of Ups-2" />
                        <Text text="{appView>noOfUps-2}" />
                        <Label  design="Bold" text="No. of Ups-3" />
                        <Text text="{appView>noOfUps-3}" />
                        <Label  design="Bold" text="No. of Sheets-1" />
                        <Text text="{appView>noOfSheets-1}" />
                        <Label  design="Bold" text="No. of Sheets-2" />
                        <Text text="{appView>noOfSheets-2}" />
                        <Label  design="Bold" text="Wastage" />
                        <Text text="{appView>wastage}" />
                            <Label  design="Bold" text="Weight (in Kgs)" />
                            <Text text="{appView>wtKgs}" />
                            <Label  design="Bold" text="Printing Sheet Size (L x W) 1" />
                            <Text text="{appView>printingSheetSizeL1} x {appView>printingSheetSizeW1}" />
                            <Label  design="Bold" text="Printing Sheet Size (L x W) 2" />
                            <Text text="{appView>printingSheetSizeL2} x {appView>printingSheetSizeW2}" />
                            <Label  design="Bold" text="Printing Machine" />
                            <Text text="{appView>printingMachine}" />
                            <Label  design="Bold" text="Punching Machine" />
                            <Text text="{appView>punchingMachine}" />
                            <Label  design="Bold" text="Pasting Machine" />
                            <Text text="{appView>pastingMachine}" />
                            <Label  design="Bold" text="Reference" />
                            <Text text="{appView>ref}" />
                            <Label  design="Bold" text="Old" />
                            <Text text="{appView>old}" />
                            <Label  design="Bold" text="None" />
                            <Text text="{appView>none}" />
                            <Label  design="Bold" text="B-2-A" />
                            <Text text="{appView>b-2-A}" />
                            <Label  design="Bold" text="None" />
                            <Text text="{appView>none }" />
                            <Label  design="Bold" text="127 Level 3" />
                            <Text text="{appView>127Level3}" />
                            <Label  design="Bold" text="None 1" />
                            <Text text="{appView>none1 }" />
                            <Label  design="Bold" text="None 2" />
                            <Text text="{appView>none2 }" />
                            <Label  design="Bold" text="13 Level 2" />
                            <Text text="{appView>13Level2}" />
                            <Label  design="Bold" text="Batch No." />
                            <Text text="{appView>batchNo}" />
                            <Label  design="Bold" text="Mfg. Date" />
                            <Text text="{appView>mfgDate}" />
                            <Label  design="Bold" text="Exp. Date" />
                            <Text text="{appView>expDate}" />
                            <Label  design="Bold" text="Corrections in Artwork" />
                            <Text text="{appView>correctionsInArtwork}" />
                            <Label  design="Bold" text="Remarks" />
                            <Text text="{appView>remarks}" />
                            <Label  design="Bold" text="Total A + B" />
                            <Text text="{appView>totalA+B}" />
                            <Label  design="Bold" text="Profit" />
                            <Text text="{appView>profit}" />
                            <Label  design="Bold" text="Total Cost of Job" />
                            <Text text="{appView>totalCostOfJob}" />
                            <Label  design="Bold" text="Cost Per Piece" />
                            <Text text="{appView>costPerPc}" />
                            <Label design="Bold"  text="Plate" />
                            <Text text="{appView>plate}" />
                            <Label design="Bold"  text="Pantone Inks" />
                            <Text text="{appView>pantoneInks}" />
                            <Label  design="Bold" text="Foil Blocks" />
                            <Text text="{appView>foilBlocks}" />
                            <Label  design="Bold" text="Positive" />
                            <Text text="{appView>positive}" />
                            <Label  design="Bold" text="Emboss Block" />
                            <Text text="{appView>embossBlock}" />
                            <Label  design="Bold" text="Punch" />
                            <Text text="{appView>punch}" />
                            <Label design="Bold"  text="Reference" />
                            <Text text="{appView>reference}" />
                            <Label  design="Bold" text="Carton Length" />
                            <Text text="{appView>cartonLength}" />
                            <Label  design="Bold" text="Carton Width" />
                            <Text text="{appView>cartonWidth}" />
                            <Label  design="Bold" text="Paper Cost" />
                            <Text text="{appView>paperCost}" />
                            <Label  design="Bold" text="Printing Charges" />
                            <Text text="{appView>printingCharges}" />
                            <Label design="Bold"  text="Varnish/Lamination" />
                            <Text text="{appView>varnish/Lamination}" />
                            <Label  design="Bold" text="Embossing" />
                            <Text text="{appView>embossing}" />
                            <Label  design="Bold" text="Punching" />
                            <Text text="{appView>punching}" />
                            <Label  design="Bold" text="B/SO Pasting" />
                            <Text text="{appView>bSOPasting}" />
                            <Label  design="Bold" text="LBTO Pasting" />
                            <Text text="{appView>lBTOPasting}" />
                            <Label  design="Bold" text="Packing" />
                            <Text text="{appView>packing}" />
                            <Label  design="Bold" text="Transportation" />
                            <Text text="{appView>transportation}" />
                            <Label  design="Bold" text="Total" />
                            <Text text="{appView>total}" />
                            <Label design="Bold"  text="Plate Charges" />
                            <Text text="{appView>plateCharges}" />
                            <Label  design="Bold" text="Blanket Charges" />
                            <Text text="{appView>blanketCharges}" />
				
				</f:content>
			</f:SimpleForm> -->
            
        <!-- </VBox> -->
        </content>
        <footer>
            <Toolbar >
               
                    
                    <ToolbarSpacer/>
                    <Button type="Emphasized" icon="sap-icon://save" visible="{appView>/onSavePayloadVis}"  text="Save" press="onSavePayload"></Button>
                    <Button type="Emphasized" icon="sap-icon://save" visible="{appView>/onUpdateJobVis}" text="Update" press="onUpdateJob"></Button>
                    <Button type="Reject" text="Clear"  icon="sap-icon://clear-all" press="onPressClear"></Button>

               
            </Toolbar>
        </footer>
    </Page>
</mvc:View>

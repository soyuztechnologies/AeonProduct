<mvc:View controllerName="ent.ui.ecommerce.controller.App" displayBlock="false" height="100%"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    xmlns:fiori="sap.ui.webc.fiori"
    xmlns:tnt="sap.tnt">
    <tnt:ToolPage sideExpanded="{appView>/sideNavExpended}">
        
        <tnt:header >
            <OverflowToolbar height="100%" width="100%" id="otb3">
               
                   <Button visible="{appView>/hamburgerVisibility}"
                    press="onClickMenuButton"
                    class="sapUiTinyMarginEnd"
                   icon="sap-icon://menu2" >
                   <layoutData>
                    <OverflowToolbarLayoutData priority="NeverOverflow" />
                    </layoutData>
                   </Button>
                   <Image src="logo/logo.png" width="4%"/>
                   <Title text="Aeon Products"></Title>
                   <ToolbarSpacer/> 
                        <!-- <core:Icon src="sap-icon://person-placeholder"  class="sapUiTinyMarginEnd">
                        </core:Icon> -->
                        <!-- <Title wrapping="true" text="{appView>/UserEmail}({appView>/userRole})" >
                            <layoutData>
                                <OverflowToolbarLayoutData priority="AlwaysOverflow" />
                            </layoutData>
                        </Title> -->
                        <Button visible="{appView>/userRoleVis}" enabled="false" wrapping="true" text="{appView>/UserEmail}({appView>/userRole})" >
                            <layoutData>
                                <OverflowToolbarLayoutData priority="High" />
                            </layoutData>
                        </Button>
               
                   <Button press="onPressLogout" text="Logout" icon="sap-icon://log" class="sapUiTinyMarginEnd"  visible="{appView>/logoutVisibility}">
                    <layoutData>

                        <OverflowToolbarLayoutData priority="Low" />
        
                    </layoutData>
                  </Button>
               
            </OverflowToolbar>
        </tnt:header>

        <tnt:mainContents>
                <f:FlexibleColumnLayout id="layout" layout="{appView>/layout}" backgroundDesign="Translucent" beginColumnNavigate="onNavigation" />
        </tnt:mainContents>

        <tnt:sideContent>
            <tnt:SideNavigation bindingContexts="true"  expanded="{appView>/sideNavExpended}" id="sideNavigation" visible="{appView>/visibility}"  itemSelect="onSelectItem">
                <tnt:NavigationList>
                    <tnt:NavigationListItem visible="{=${appView>/UserRole} === 'Admin' || ${appView>/UserRole} === 'Factory Manager' ? true : false}" text="Upload / Manage Documents" icon="sap-icon://upload"  key="UploadXml"/>
                    <tnt:NavigationListItem visible="{=${appView>/UserRole} === 'Admin' || ${appView>/UserRole} === 'Factory Manager' || ${appView>/UserRole} === 'Customer' || ${appView>/UserRole} === 'Raw Material Head' || ${appView>/UserRole} === 'Prinitng Head' || ${appView>/UserRole} === 'Post Press Head' || ${appView>/UserRole} === 'Dispatch Head' || ${appView>/UserRole} === 'Accounts Head' || ${appView>/UserRole} === 'Artwork Head' ? true : false }" text="All Printers" icon="sap-icon://business-card" key="allPrinters"/>
                    <tnt:NavigationListItem visible="{=${appView>/UserRole} === 'Admin' || ${appView>/UserRole} === 'Factory Manager' ? true : false }" text="User Details" icon="sap-icon://family-care" key="userDetails"/>
                    <tnt:NavigationListItem visible="{=${appView>/UserRole} === 'Admin' || ${appView>/UserRole} === 'Factory Manager' || ${appView>/UserRole} === 'Customer' || ${appView>/UserRole} === 'Raw Material Head' || ${appView>/UserRole} === 'Prinitng Head' || ${appView>/UserRole} === 'Post Press Head' || ${appView>/UserRole} === 'Dispatch Head' || ${appView>/UserRole} === 'Accounts Head' || ${appView>/UserRole} === 'Artwork Head' ? true : false }" text="Profile" icon="sap-icon://customer" key="Profile"/>
                </tnt:NavigationList>
            </tnt:SideNavigation>
        </tnt:sideContent>

    </tnt:ToolPage>
</mvc:View>
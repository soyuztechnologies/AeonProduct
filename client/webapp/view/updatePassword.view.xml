<mvc:View
	controllerName="ent.ui.ecommerce.controller.updatePassword"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc">

<MessagePage
	title="{appView>/messagePageTextUpdate}"
	text="{appView>/messagePageTextUpdate}"
	description="{appView>/timerTextUpdate}"
	icon="{appView>/updateIcon}">
	<buttons>
		<Button text="Logout" press="onMessagePageLogout" visible="{appView>/updateLogout}"></Button>
	</buttons>
</MessagePage>
<Text text=";adskhfdsa;fdsa;fkh"></Text>
</mvc:View>